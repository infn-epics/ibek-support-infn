# yaml-language-server: $schema=../../ibek-support/_scripts/support_install_variables.json

module: chiller-polyscience

# TODO this should be a release tag to ensure deterministic builds.
version: main

# this module uses stream device so requires none of its own libs/dbds
organization: https://github.com/infn-epics/

dbds:
  - asyn.dbd
  - stream.dbd

libs:
  - stream
  - asyn

protocol_files:
  - iocBoot/polyscience-ioc/polyscience.proto
